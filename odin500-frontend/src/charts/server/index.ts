/**
 * Server-rendered chart primitives (SVG in HTML).
 * Client canvas charts hydrate on top via ChartEnhanceSlot.
 */
export { SvgSparkline } from '@/seo/charts/SvgSparkline';
export { SvgBarChart } from '@/seo/charts/SvgBarChart';
